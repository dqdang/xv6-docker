(ps) A ps command that lists processes

DONE KINDA = (ADD IN DISK ERROR CHECK (free) A free command that show the available 

DONE KINDA = (ADD IN MEM ERROR CHECK) (df) Disk space usage

DONE = (cinfo) A cinfo command that can only execute in the root container

DONE = (ccreate) Create a container and populate a container FS

DONE = (cstart) Start a container - give a virtual console, container directory, an initial 

(cpause) Pause a container

(cresume) Resume a container

(cstop) Stop a container

(cadd) adds executable to container

